> **⚠️ 这份计划书已被取代。**
>
> 它记录的是项目早期的「文献可视化工具」方向：导入 Zotero、画全量图谱、做筛选统计。
> 现在的 Yantu 是一个**游戏化的领域探索工具**——默认隐藏未知，用迷雾和点亮机制引导
> 用户走出自己已经熟悉的区域。
>
> 当前设计见 [`docs/design.md`](docs/design.md)，上手见 [`README.md`](README.md)。
>
> 保留本文档是因为其中的数据模型、导入格式、去重策略、隐私原则和跨平台要求仍然有效，
> 且大部分已经实现。第 4.4 节的图谱交互、第 8 节的阶段划分和第 14 节的 MVP 定义
> 已不再适用。

---

# Yantu (研图)学术地图软件项目计划书

## 1. 项目概述

### 1.1 项目名称

**Yantu —— 个人学术地图（Academic Map）**

### 1.2 项目定位

Yantu 是一款面向研究生、科研人员、教师和知识工作者的跨平台桌面软件，支持从 Zotero 及其他文献管理工具导入论文基本信息，通过分析论文之间的引用、作者、关键词、期刊、主题和时间关系，构建可交互的个人学术地图。

软件的核心目标不是简单地展示论文列表，而是帮助用户回答以下问题：

- 我正在研究哪些主题？
- 不同研究方向之间有什么联系？
- 哪些论文是某个领域的基础文献或关键节点？
- 我的阅读范围是否存在明显空白？
- 某个研究主题是如何发展和演变的？
- 哪些作者、机构或研究群体与我的文献库关系密切？

### 1.3 目标平台

- Windows
- macOS
- Linux

软件采用桌面端优先的设计，数据默认保存在本地，保护用户的文献库和研究隐私。

---

## 2. 项目目标

### 2.1 总体目标

制作一款稳定、易用、隐私友好的跨平台学术文献可视化工具，使用户能够：

1. 导入个人论文库；
2. 自动整理和规范化论文信息；
3. 建立论文之间的多维关系；
4. 以图谱方式浏览个人知识结构；
5. 通过筛选、搜索和时间轴分析研究轨迹；
6. 持续同步或更新论文数据。

### 2.2 MVP 阶段目标

第一阶段只聚焦于“导入文献并生成基础学术地图”：

- 支持 Zotero 导出文件导入；
- 支持 BibTeX、RIS、CSV、JSON 等通用格式；
- 解析论文标题、作者、摘要、关键词、年份、DOI、期刊、标签、收藏夹等信息；
- 自动去重和字段规范化；
- 根据关键词、标签、作者和引用关系建立论文关联；
- 提供节点图谱和基本交互；
- 支持按主题、年份、作者、标签等条件筛选；
- 支持保存和重新打开本地项目。

---

## 3. 用户画像与使用场景

### 3.1 目标用户

#### 用户 A：研究生

希望快速了解自己的阅读范围、发现研究空白，并整理毕业论文相关文献。

#### 用户 B：科研人员

已有较大的 Zotero 文献库，需要观察不同项目、研究主题和合作网络之间的关系。

#### 用户 C：文献综述作者

需要梳理领域发展脉络、识别关键论文和研究流派。

#### 用户 D：跨领域学习者

希望通过论文之间的主题和引用关系建立某一新领域的整体认知。

### 3.2 典型使用流程

 1. 用户安装 Yantu；
 2. 创建一个新的学术地图项目；
 3. 从 Zotero 导出或导入 BibTeX/RIS/CSV 文件；
 4. 软件解析并展示导入预览；
 5. 用户确认字段映射和去重结果；
 6. 软件建立论文节点及节点之间的关系；
 7. 用户在图谱中按主题、年份或作者浏览；
 8. 点击节点查看论文详细信息；
 9. 用户修改标签、添加备注或调整关系；
10. 保存项目并在后续导入新文献进行更新。

---

## 4. 核心功能规划

## 4.1 文献导入

### 第一阶段支持

- Zotero CSV 导出文件；
- BibTeX（`.bib`）；
- RIS（`.ris`）；
- CSV（`.csv`）；
- JSON（`.json`）；
- 手动录入单篇论文；
- 粘贴 DOI 或论文链接进行补充。

### 可选导入方式

- 读取 Zotero 数据库的只读副本；
- Zotero Better BibTeX 导出文件；
- 从文件夹批量导入 PDF，并尝试提取 DOI；
- 从 Notion、EndNote、Mendeley 等工具导入；
- 通过浏览器扩展发送论文信息到 Yantu。

### 导入预览

导入前显示：

- 论文总数；
- 可识别和无法识别的记录数；
- 重复记录数；
- 缺少标题、作者、年份或 DOI 的记录；
- 字段映射结果；
- 需要用户处理的异常数据。

---

## 4.2 文献数据规范化

导入后对数据进行统一处理：

- 标题大小写和空格规范化；
- 作者姓名格式统一；
- DOI 格式清理；
- 期刊名称统一；
- 关键词分隔符统一；
- 年份转为标准数字格式；
- 合并同一论文的重复记录；
- 记录原始数据，保证可追溯；
- 对缺失字段进行标记，而不是静默丢弃。

### 去重策略

按照以下优先级判断重复记录：

1. DOI 完全一致；
2. 标题标准化后完全一致；
3. 标题相似度、作者和年份综合判断；
4. 用户手动确认合并结果。

---

## 4.3 论文关系构建

论文之间的关系分为显式关系和推断关系。

### 显式关系

这些关系可以直接从导入数据或外部元数据中获得：

- 引用关系；
- 被引用关系；
- 共同作者；
- 相同 DOI 或关联版本；
- Zotero 收藏夹关系；
- Zotero 标签关系。

### 推断关系

这些关系由数据分析算法计算得到：

- 关键词相似；
- 标题语义相似；
- 摘要语义相似；
- 相同作者或机构；
- 相同期刊或会议；
- 时间邻近；
- 主题聚类相近。

### 关系权重

每条关系应保存一个 0 至 1 的权重，用于控制图谱中连线的强弱和筛选：

```text
关系权重 = 引用关系权重
         + 关键词相似度
         + 摘要相似度
         + 作者重合度
         + 标签重合度
```

具体权重在实现阶段通过配置项调整，并允许用户选择只查看某一种关系。

---

## 4.4 学术地图可视化

### 图谱元素

- 论文：基础节点；
- 主题：聚类节点或区域；
- 作者：可选节点类型；
- 机构：可选节点类型；
- 期刊或会议：可选节点类型；
- 时间：通过时间轴或颜色表达；
- 关系：节点之间的连线。

### 节点视觉编码

- 节点大小：引用次数、关联度或用户重要性；
- 节点颜色：主题、标签或研究方向；
- 节点形状：论文、作者、主题等实体类型；
- 节点边框：是否已阅读、是否收藏、是否需要重点关注；
- 连线粗细：关系强度；
- 连线样式：引用、作者、关键词或推断关系。

### 基础交互

- 缩放；
- 平移；
- 拖动节点；
- 框选节点；
- 单击查看摘要信息；
- 双击打开详细信息；
- 隐藏或显示关系类型；
- 聚焦某篇论文及其邻居；
- 锁定节点位置；
- 自动布局；
- 导出当前地图为 PNG、SVG 或 PDF。

### 地图模式

第一阶段：

- 论文关系图；
- 主题聚类图；
- 年份时间轴；
- 作者关系图。

后续阶段：

- 引用网络图；
- 研究方向演化图；
- 个人阅读轨迹图；
- 机构和合作网络图；
- 多项目对比图。

---

## 4.5 论文详情页

点击论文节点后显示：

- 标题；
- 作者；
- 摘要；
- 关键词；
- 发表年份；
- DOI；
- 期刊或会议；
- 机构信息；
- Zotero 标签和收藏夹；
- 引用数和被引用论文；
- 关联论文；
- 用户备注；
- 阅读状态；
- 原文链接；
- 本地 PDF 路径（如果存在）。

支持用户修改：

- 标签；
- 阅读状态；
- 重要程度；
- 个人备注；
- 自定义主题；
- 人工添加或删除关系。

---

## 4.6 搜索、筛选与分析

### 搜索

支持按照以下字段搜索：

- 标题；
- 作者；
- DOI；
- 摘要；
- 关键词；
- 标签；
- 期刊；
- 备注。

### 筛选

- 发表年份区间；
- 作者；
- 主题；
- Zotero 收藏夹；
- 标签；
- 阅读状态；
- 重要程度；
- 是否有摘要；
- 是否有 DOI；
- 关系类型；
- 最小关联度。

### 统计面板

- 论文总数；
- 年度论文数量；
- 主题数量和占比；
- 高频关键词；
- 高度连接论文；
- 主要作者；
- 主要期刊或会议；
- 未分类论文；
- 近年新增论文；
- 阅读完成度。

---

## 4.7 数据更新与同步

### 本地更新

- 重复导入时自动识别已有记录；
- 仅更新发生变化的字段；
- 保留用户自定义字段；
- 提供导入变更预览；
- 支持撤销本次导入。

### 外部元数据补全

可选接入以下服务：

- Crossref；
- OpenAlex；
- Semantic Scholar；
- Europe PMC；
- arXiv；
- DOI 内容协商接口。

外部服务只用于补全文献元数据。默认不上传用户完整文献库，需明确告知用户并允许关闭联网功能。

---

## 5. 非功能需求

### 5.1 跨平台

- Windows 10/11；
- macOS 12 及以上，兼容 Intel 和 Apple Silicon；
- 主流 Linux 发行版，优先支持 Ubuntu 22.04 及以上；
- 提供安装包和便携版（Windows 可选）；
- 遵循各平台的应用目录和权限规范。

### 5.2 性能目标

MVP 阶段目标：

- 1,000 篇论文导入时间不超过 10 秒；
- 10,000 篇论文可以完成数据检索和基本筛选；
- 1,000 个节点的关系图交互保持流畅；
- 大型图谱采用按需渲染、聚类和渐进加载；
- 导入、分析和布局计算不阻塞界面。

### 5.3 隐私与安全

- 数据默认本地存储；
- 不上传用户的完整文献库；
- 外部 API 请求可关闭；
- API Key 使用系统安全存储；
- 导入文件只读解析，不修改原始文件；
- 项目支持加密备份（后续版本）；
- 清晰展示联网服务及发送的数据字段。

### 5.4 可用性

- 首次启动提供示例项目；
- 导入流程提供向导；
- 对解析失败的数据给出明确提示；
- 支持中英文界面；
- 支持键盘快捷键；
- 支持深色和浅色主题；
- 重要操作提供撤销功能。

---

## 6. 技术方案

## 6.1 总体架构

```text
┌─────────────────────────────────────────────┐
│                 React 前端                  │
│  导入界面 | 图谱界面 | 搜索筛选 | 详情面板   │
└───────────────────┬─────────────────────────┘
                    │ Tauri Commands / Events
┌───────────────────▼─────────────────────────┐
│                 Rust 核心层                  │
│ 导入解析 | 数据规范化 | 去重 | 图谱计算       │
│ 文件管理 | 数据库访问 | 外部 API | 导出       │
└───────────────────┬─────────────────────────┘
                    │
┌───────────────────▼─────────────────────────┐
│                 本地数据层                  │
│ SQLite 数据库 | 项目文件 | 缓存 | 日志       │
└─────────────────────────────────────────────┘
```

### 6.2 前端技术

当前项目已经采用：

- React；
- TypeScript；
- Vite；
- Tauri API。

建议新增：

- 图谱：Cytoscape.js、Sigma.js 或 AntV G6；
- 状态管理：Zustand；
- UI 组件：Tailwind CSS 或 Mantine；
- 数据校验：Zod；
- 测试：Vitest、React Testing Library、Playwright。

图谱库的最终选择需要重点比较大规模节点性能、布局算法、导出能力和 Tauri 兼容性。

### 6.3 Rust 核心层

建议模块划分：

```text
src-tauri/src/
├── main.rs              # 应用入口
├── commands/            # Tauri 命令
├── models/              # 数据结构
├── importers/            # BibTeX/RIS/CSV/JSON 解析
├── normalize/            # 字段规范化和去重
├── graph/                # 关系构建和图谱计算
├── database/             # SQLite 访问
├── metadata/             # Crossref/OpenAlex 等接口
├── export/               # 图片、JSON、GraphML 等导出
└── error.rs              # 统一错误处理
```

### 6.4 数据库

推荐使用 SQLite，原因：

- 适合本地单用户应用；
- 跨 Windows、macOS、Linux；
- 支持索引和全文搜索；
- 易于备份和迁移；
- 不需要单独部署数据库服务。

可使用 Rust 的 `sqlx` 或 `rusqlite` 访问。

---

## 7. 数据模型设计

### 7.1 papers 表

```text
papers
- id
- title
- abstract
- year
- doi
- url
- journal
- volume
- issue
- pages
- publisher
- language
- source
- source_key
- pdf_path
- reading_status
- importance
- created_at
- updated_at
```

### 7.2 authors 表

```text
authors
- id
- display_name
- family_name
- given_name
- orcid
- affiliation
```

### 7.3 paper_authors 表

```text
paper_authors
- paper_id
- author_id
- author_order
```

### 7.4 tags 表和 paper_tags 表

```text
tags
- id
- name
- color

paper_tags
- paper_id
- tag_id
- source
```

### 7.5 relations 表

```text
relations
- id
- source_paper_id
- target_paper_id
- relation_type
- weight
- confidence
- source
- created_at
```

`relation_type` 示例：

- `cites`：引用；
- `co_author`：共同作者；
- `keyword`：关键词相似；
- `semantic`：语义相似；
- `same_topic`：同一主题；
- `manual`：用户手动关系。

### 7.6 projects 表

```text
projects
- id
- name
- description
- data_version
- created_at
- updated_at
```

---

## 8. 分阶段开发计划

## 阶段 0：项目准备（1 周）

### 工作内容

- 确认产品范围和 MVP；
- 确定图谱库和数据库方案；
- 建立代码目录和模块规范；
- 配置 lint、format、单元测试和构建流程；
- 设计基础 UI 原型；
- 准备测试用论文数据集。

### 交付物

- 产品需求文档；
- 技术设计文档；
- UI 原型；
- 可运行的 Tauri 项目基础框架。

## 阶段 1：文献导入与本地存储（2 周）

### 工作内容

- 建立 SQLite 数据库；
- 实现论文、作者、标签、项目数据模型；
- 实现 BibTeX、RIS、CSV、JSON 解析；
- 实现导入预览；
- 实现去重和错误提示；
- 实现项目保存和重新打开。

### 验收标准

- 能成功导入 1,000 条以上论文记录；
- 导入结果可预览、确认和取消；
- 重复论文可以被识别；
- 关闭软件后数据不会丢失；
- 原始导入文件不会被修改。

## 阶段 2：基础图谱（2 周）

### 工作内容

- 生成论文节点；
- 根据作者、标签、关键词建立初始关系；
- 接入图谱可视化组件；
- 实现缩放、平移、拖拽和节点详情；
- 实现基础布局算法；
- 支持图谱状态保存。

### 验收标准

- 导入论文后可以生成图谱；
- 点击节点可以查看论文信息；
- 可以隐藏或显示关系；
- 1,000 个节点规模下界面基本可用；
- 图谱不存在无法恢复的崩溃。

## 阶段 3：搜索、筛选与主题分析（2 周）

### 工作内容

- 实现全文搜索；
- 增加年份、标签、作者、主题筛选；
- 增加统计面板；
- 实现关键词聚类；
- 增加时间轴；
- 实现主题颜色和图例。

### 验收标准

- 用户可以快速定位目标论文；
- 筛选结果能够同步更新图谱；
- 用户可以识别主要主题和年度变化；
- 搜索和筛选不会修改原始数据。

## 阶段 4：外部元数据和引用关系（2 周）

### 工作内容

- 接入 Crossref 或 OpenAlex；
- 根据 DOI 补全文献信息；
- 获取引用和被引用关系；
- 设计网络请求失败和限流处理；
- 增加联网开关和隐私提示；
- 缓存已经获取的元数据。

### 验收标准

- 用户可以手动触发元数据补全；
- 网络失败时软件仍可正常使用本地数据；
- 用户能看到哪些字段来自外部服务；
- 不经用户同意不上传完整文献库。

## 阶段 5：导出、体验优化和发布（2 周）

### 工作内容

- 导出 PNG、SVG、PDF；
- 导出 JSON、CSV、GraphML 等数据格式；
- 完善中英文界面；
- 增加深色模式；
- 完善错误提示和日志；
- 构建 Windows、macOS、Linux 安装包；
- 编写用户手册和隐私政策；
- 进行跨平台测试。

### 验收标准

- 三个平台均能安装和启动；
- 核心数据可以备份和恢复；
- 图谱可以用于报告或论文展示；
- 关键操作有明确反馈；
- 发布包可以完成版本升级。

---

## 9. 后续版本规划

### 9.1 版本 1.1

- 直接读取 Zotero 数据库副本；
- 监控 Zotero 导出文件变化；
- 批量 PDF 元数据识别；
- 自定义字段；
- 更强的图谱布局；
- 图谱节点手动编辑。

### 9.2 版本 1.2

- 语义向量和本地嵌入模型；
- 自动主题命名；
- 相似论文推荐；
- 研究空白提示；
- 论文阅读进度分析；
- 文献综述辅助视图。

### 9.3 版本 2.0

- 可选云同步；
- 团队共享项目；
- 多人协作标注；
- 浏览器扩展；
- Zotero 插件；
- 远程备份；
- AI 辅助论文摘要、分类和关系解释。

---

## 10. 关键技术难点与解决方案

### 10.1 不同来源字段不统一

**问题：** BibTeX、RIS、Zotero CSV 的字段名称和数据结构不同。

**方案：** 建立统一的中间数据结构，为每个导入器编写字段映射和规范化逻辑，并保留原始字段。

### 10.2 论文去重不准确

**问题：** 同一论文可能存在预印本、正式出版版本、不同语言版本或多个导出记录。

**方案：** 优先使用 DOI，结合标题相似度、作者、年份进行辅助判断，所有自动合并均提供用户确认和撤销。

### 10.3 引用数据不完整

**问题：** 某些论文没有 DOI，或外部数据库没有完整引用信息。

**方案：** 将引用关系作为增强能力，而不是 MVP 的唯一基础。即使没有引用信息，也可以依据作者、关键词、标签和语义相似度生成基础地图。

### 10.4 大规模图谱性能

**问题：** 节点和连线过多时，浏览器渲染及布局计算会变慢。

**方案：**

- 使用 WebGL 图谱渲染；
- 根据缩放级别显示不同细节；
- 支持主题聚类和节点折叠；
- 对关系设置最小权重阈值；
- 将复杂计算放到 Rust 后台线程；
- 对结果进行缓存。

### 10.5 外部服务依赖

**问题：** API 限流、服务不可用或数据授权政策变化会影响功能。

**方案：** 使用适配器接口隔离不同服务，所有外部数据本地缓存，网络不可用时保证离线核心功能可用。

### 10.6 跨平台差异

**问题：** 文件路径、权限、字体、窗口行为和安装方式在不同系统上存在差异。

**方案：** 使用 Tauri 的跨平台能力和 Rust 标准库处理文件系统；在 CI 中分别构建和测试各个平台。

---

## 11. 测试计划

### 11.1 单元测试

- BibTeX、RIS、CSV、JSON 解析；
- DOI 清洗；
- 作者姓名规范化；
- 标题相似度计算；
- 去重逻辑；
- 关系权重计算；
- 数据库增删改查。

### 11.2 集成测试

- 导入文件到数据库；
- 导入后生成图谱；
- 外部元数据补全；
- 项目保存和恢复；
- 数据导出和重新导入。

### 11.3 UI 测试

- 新建项目；
- 导入文献；
- 处理重复记录；
- 搜索和筛选；
- 查看论文详情；
- 导出图谱。

### 11.4 跨平台测试

至少测试：

- Windows 10/11；
- macOS Intel；
- macOS Apple Silicon；
- Ubuntu Linux。

### 11.5 测试数据集

准备三组数据：

- 小型数据集：50 篇论文，用于快速开发测试；
- 中型数据集：1,000 篇论文，用于性能测试；
- 大型数据集：10,000 篇以上论文，用于压力测试。

---

## 12. 版本和发布策略

### 12.1 版本命名

采用语义化版本：

```text
主版本.次版本.修订版本
例如：0.1.0、0.2.0、1.0.0
```

### 12.2 初始版本建议

- `0.1.0`：项目骨架和示例数据；
- `0.2.0`：多格式导入和本地数据库；
- `0.3.0`：基础论文图谱；
- `0.4.0`：搜索、筛选和主题分析；
- `0.5.0`：引用关系和外部元数据；
- `1.0.0`：跨平台稳定版。

### 12.3 发布渠道

- GitHub Releases；
- Windows：MSI、NSIS；
- macOS：DMG；
- Linux：AppImage、Deb，后续可增加 RPM；
- 自动构建：GitHub Actions。

---

## 13. 项目目录演进建议

当前项目是 React + Tauri 默认模板，建议逐步演进为：

```text
.
├── src/
│   ├── components/
│   │   ├── layout/
│   │   ├── import/
│   │   ├── graph/
│   │   ├── paper/
│   │   └── filters/
│   ├── pages/
│   ├── hooks/
│   ├── stores/
│   ├── types/
│   ├── services/
│   ├── App.tsx
│   └── main.tsx
├── src-tauri/
│   └── src/
│       ├── commands/
│       ├── database/
│       ├── importers/
│       ├── graph/
│       ├── metadata/
│       ├── models/
│       └── main.rs
├── data/
│   └── sample-library/
├── docs/
│   ├── architecture.md
│   ├── data-model.md
│   └── user-guide.md
└── 计划书.md
```

---

## 14. MVP 完成定义

当以下条件全部满足时，认为 MVP 完成：

- [ ] 可以在 Windows、macOS、Linux 上安装并启动；
- [ ] 可以创建、保存和重新打开项目；
- [ ] 可以导入 Zotero CSV、BibTeX、RIS、CSV 或 JSON 文件；
- [ ] 可以展示导入预览和错误信息；
- [ ] 可以识别基础重复记录；
- [ ] 可以查看论文列表和论文详情；
- [ ] 可以根据作者、标签、关键词建立论文关系；
- [ ] 可以显示论文关系图谱；
- [ ] 可以搜索和筛选论文；
- [ ] 可以按年份和主题查看统计结果；
- [ ] 可以手动编辑标签、阅读状态和备注；
- [ ] 可以导出图谱图片；
- [ ] 网络不可用时，导入、浏览和基础图谱功能仍然可用；
- [ ] 不会未经用户同意上传完整文献库。

---

## 15. 成功指标

### 产品指标

- 新用户从启动到生成第一张地图不超过 5 分钟；
- 1,000 篇论文导入成功率达到 95% 以上；
- 用户可以在 10 秒内定位任意已导入论文；
- 用户可以通过图谱发现至少一种非列表形式的文献关系；
- 核心功能在三大桌面平台上保持一致。

### 用户体验指标

- 导入失败时能够明确说明原因；
- 论文关系的来源和可信度可解释；
- 自动分析结果不会覆盖用户手工编辑；
- 用户能够方便地撤销导入、合并和删除操作；
- 软件在无网络环境下仍具备基本使用价值。

---

## 16. 第一批开发任务

建议按照以下顺序开始实施：

 1. 将默认 Tauri 欢迎页替换为 Yantu 主界面；
 2. 设计左侧导航、顶部工具栏和右侧详情面板；
 3. 定义 `Paper`、`Author`、`Tag`、`Relation` TypeScript 类型；
 4. 准备一组示例论文 JSON 数据；
 5. 开发论文列表和论文详情界面；
 6. 实现 BibTeX、RIS、CSV 导入预览；
 7. 在 Rust 中建立 SQLite 数据库；
 8. 接入图谱组件并展示示例关系；
 9. 实现搜索和基础筛选；
10. 将前端默认状态替换为真实项目数据。

---

## 17. 产品原则

1. **本地优先**：用户的文献数据首先属于用户本人。
2. **渐进增强**：没有外部 API 时，基础功能仍然可用。
3. **结果可解释**：每条关系都应尽可能说明来源。
4. **自动化可撤销**：任何自动去重、分类和分析都不能破坏原始数据。
5. **图谱服务于研究**：可视化不是目的，帮助用户理解研究结构才是目的。
6. **跨平台一致**：不同操作系统上的核心操作和数据格式保持一致。
7. **从简单开始**：先完成可靠的导入、检索和地图，再逐步加入 AI 和协作能力。

